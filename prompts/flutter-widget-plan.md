# Prompt: Flutter Widget Plan

Plan for a new Flutter widget/screen.

- Identify if `Stateless` or `Stateful`.
- List required `Theme` tokens and `Assets`.
- Define the `build` method structure in bullets.
- Check for responsiveness (LayoutBuilder/MediaQuery).
